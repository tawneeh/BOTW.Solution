@{
  Layout = "_Layout";
}

@model BOTW.Models.Character;

<h4>Create a Character</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br><span class="glyphicon glyphicon-user" aria-hidden="true"></span><br><input type="submit" value="Add Character">
}